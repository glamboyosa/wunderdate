query ($id: Int!) @rbac(requireMatchAll: [admin]) {
  getApplicationsWithQuery: Nexus_getApplication(id: $id) {
    id
    message
    status
    role {
      description
      id
      name
    }
  }
}