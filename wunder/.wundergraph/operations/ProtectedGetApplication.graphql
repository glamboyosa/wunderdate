query ($id: Int!) @rbac(requireMatchAll: [admin]) {
  getApplication: Nexus_getApplication(id: $id) {
    id
    message
    status
    users {
      userId
      user {
        name
      }
    }
    role {
      description
      id
      name
    }
  }
}
