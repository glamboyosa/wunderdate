query @rbac(requireMatchAll: [admin]) {
  getPositions: Nexus_getPositions {
    id
    open
    name
    description
    application {
      message
      role {
        name
        description
      }
    }
  }
}
